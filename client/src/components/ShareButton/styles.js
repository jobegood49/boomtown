const styles = theme => ({
  button: {
    boxShadow: 'none'
  },
  addButton: {
    color: theme.palette.primary.main,
    backgroundColor: theme.palette.secondary.main,
    borderRadius: '50px',
    margin: '10px'
  }
})

export default styles
